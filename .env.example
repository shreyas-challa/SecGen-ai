# ============================================================
# AI Security Research Agent — Environment Configuration
# Copy this file to .env and fill in your values.
# NEVER commit your .env file to version control.
# ============================================================

# --- LLM Provider --------------------------------------------
# "anthropic"  = direct Anthropic API (may hit 10k TPM rate limits on free tier)
# "openrouter" = OpenRouter API (higher rate limits, hosts Claude models)
# "dedalus"    = Dedalus Labs API (OpenAI-compatible, routes to Claude/OpenAI)
PROVIDER=dedalus

# --- Anthropic API (used when PROVIDER=anthropic) ----------------
ANTHROPIC_API_KEY=sk-ant-xxxxx-your-key-here

# --- OpenRouter API (used when PROVIDER=openrouter) --------------
OPENROUTER_API_KEY=sk-or-xxxxx-your-key-here

# --- Dedalus Labs API (used when PROVIDER=dedalus) ---------------
# Get your key at: https://www.dedaluslabs.ai/dashboard/api-keys
DEDALUS_API_KEY=dsk-live-xxxxx-your-key-here

# Optional: override the Claude model
# Anthropic models:        claude-sonnet-4-6, claude-haiku-4-5-20251001
# OpenRouter/Dedalus:      anthropic/claude-sonnet-4-6, anthropic/claude-opus-4-6
# CLAUDE_MODEL=anthropic/claude-sonnet-4-6

# --- Metasploit RPC ------------------------------------------
# Start msfrpcd: msfrpcd -P yourpassword -S -a 127.0.0.1
MSF_HOST=127.0.0.1
MSF_PORT=55553
MSF_USER=msf
MSF_PASSWORD=yourpassword
MSF_SSL=false

# --- Tool Binary Paths (leave as-is if tools are in PATH) ----
NMAP_PATH=nmap
NUCLEI_PATH=nuclei
FFUF_PATH=ffuf
SQLMAP_PATH=sqlmap

# --- Wordlists -----------------------------------------------
DEFAULT_WORDLIST=/usr/share/wordlists/dirb/common.txt
SUBDOMAINS_WORDLIST=/usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt

# --- Nuclei Templates ----------------------------------------
# Leave blank to use built-in templates, or set to a custom path
NUCLEI_TEMPLATES=

# --- Agent Behaviour -----------------------------------------
# Agent mode: "htb" (aggressive, full exploitation) or "pentest" (conservative, PoC-only)
AGENT_MODE=htb

# Your attacker IP (tun0 on HTB VPN) — needed for reverse shell payloads
LHOST=
# Default listener port for reverse shells
LPORT=4444
# Default timeout in seconds for shell commands
SHELL_TIMEOUT=120

MAX_ITERATIONS=30

# Set to true to simulate all tool calls without real network activity
DRY_RUN=false

# --- Scope ---------------------------------------------------
# Comma-separated list of IPs, CIDR ranges, or domain names
# If empty, the primary target is auto-added at runtime
# ALLOWED_SCOPE=192.168.1.0/24,example.com
ALLOWED_SCOPE=

# --- Output --------------------------------------------------
OUTPUT_DIR=./output
